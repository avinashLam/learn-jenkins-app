pipeline {
    agent any

    stages {
        stage('hello') {
            
            steps {
                script {
                    sh 'echo "Starting Build Process..."'
                }
            
            }
        }
    }

    post {
        success {
            echo 'Build completed successfully.'
        }
        failure {
            echo 'Build failed!'
        }
    }
}
